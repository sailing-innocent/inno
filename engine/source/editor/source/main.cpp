#include <editor/include/editor.h>

int main(int argc, char** argv)
{
    // new engine
    // engine.config()
    // engine.init()
    // engine.run()
    // engine.clear()
    // engine.shutdown()
    inno::sayHello();
    return 0;
}